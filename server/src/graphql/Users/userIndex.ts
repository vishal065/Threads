import { typeDefs } from "./typedef"; //typedef wali
import { queries } from "./queries"; //typedef wali
import { mutations } from "./mutations"; //typedef wali
import resolvers from "./resolvers";

export const User = { typeDefs, queries, mutations, resolvers };
